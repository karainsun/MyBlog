import React, { FC } from 'react';
import { Button } from 'antd';
import './index.css';

const App: FC = () => (
  <div className="App">
    <h2>React Ts 项目初始化完成</h2>
    <Button type="primary">Button1</Button>
  </div>
);

export default App;